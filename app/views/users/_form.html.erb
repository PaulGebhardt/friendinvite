<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :facebookid %><br />
    <%= f.number_field :facebookid %>
  </div>
  <div class="field">
    <%= f.label :invites %><br />
    <%= f.number_field :invites %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
